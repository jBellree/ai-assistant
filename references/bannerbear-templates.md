# Deprecated — Bannerbear is no longer used

The social content system now uses **hcti.io** (HTML → image API) instead of Bannerbear.

Templates live as HTML files in `.claude/skills/social-content/templates/`.
Logos are base64-encoded in `references/brand/logos/*.b64` and embedded directly into the template HTML.

See the live workflow in `.claude/skills/social-content/SKILL.md`.

This file can be deleted.

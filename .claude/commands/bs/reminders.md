---
description: Show Apple Reminders — ask B which slice first, then fetch just that slice
argument-hint: [overdue|today|week|flagged|list <name>|everything]
---

Invoke the `bs-reminders` skill.

Pass `$ARGUMENTS` directly to the skill — if provided it skips the slice menu and goes straight to the fetch.
